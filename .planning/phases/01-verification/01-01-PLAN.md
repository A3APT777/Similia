# Phase 1 · UI Verification — Execution Plan
# Requirements: VERIFY-01, VERIFY-02, VERIFY-03

## Scope

1. Добавить `IntakeLinkButton` на карточку пациента (он создан но не используется)
2. Добавить `data-tour` атрибуты на все элементы, которые туры этапов 2 и 3 будут подсвечивать
3. Не менять логику, стили и тексты — только добавить атрибуты и подключить компонент
4. Запустить `npm run build` и убедиться что TypeScript не ругается

---

## Task 1 — Добавить IntakeLinkButton на карточку пациента

**Файл:** `src/app/patients/[id]/page.tsx`

Компонент `IntakeLinkButton` создан (`src/app/patients/[id]/IntakeLinkButton.tsx`) но нигде не используется.

### Шаги:
1. Прочитать `src/app/patients/[id]/IntakeLinkButton.tsx` — понять пропсы (принимает `patientId: string`)
2. Прочитать `src/app/patients/[id]/page.tsx` — найти CTA-зону рядом с `<ScheduleButton patientId={id} />` (строка 247)
3. Добавить импорт: `import IntakeLinkButton from './IntakeLinkButton'`
4. Добавить компонент рядом со `ScheduleButton` — в ту же зону CTA

### Где вставить (рядом со строкой 247):
```tsx
// Было:
<ScheduleButton patientId={id} />

// Стало:
<IntakeLinkButton patientId={id} />
<ScheduleButton patientId={id} />
```

---

## Task 2 — data-tour на карточке пациента

После Task 1 добавить атрибуты на странице пациента.

### 2a — IntakeLinkButton (`intake-link`)
**Файл:** `src/app/patients/[id]/IntakeLinkButton.tsx`

Прочитать файл, найти основную кнопку (когда `!link` — первичное состояние), добавить `data-tour="intake-link"` на неё.

### 2b — ScheduleButton (`schedule-btn`)
**Файл:** `src/components/ScheduleButton.tsx`

Найти кнопку в свёрнутом состоянии (`!open` → `<button onClick={handleOpen}...>`), добавить `data-tour="schedule-btn"`.

---

## Task 3 — data-tour в ConsultationEditor

**Файл:** `src/app/patients/[id]/consultations/[consultationId]/ConsultationEditor.tsx`

### 3a — Поле жалоб (`complaints`)
Textarea для жалоб есть в двух местах: Quick Mode и Deep Mode. Добавить `data-tour="complaints"` **в оба** места.

Найти оба места по `value={state.complaints}` — первый в блоке `state.mode === 'quick'`, второй в блоке `else`.

### 3b — Кнопка завершения (`finish-btn`)
Найти `<button onClick={handleFinish}` в sticky footer (ближе к концу файла), добавить `data-tour="finish-btn"`.

---

## Task 4 — data-tour в EditorToolbar

**Файл:** `src/app/patients/[id]/consultations/[consultationId]/components/EditorToolbar.tsx`

Прочитать файл, найти `<button ... onClick={onOpenRepertory}`, добавить `data-tour="open-repertory"`.

---

## Task 5 — data-tour в InlineRx

**Файл:** `src/app/patients/[id]/consultations/[consultationId]/components/InlineRx.tsx`

Прочитать файл, найти корневой `<div>` компонента (первый div функции), добавить `data-tour="inline-rx"`.

---

## Task 6 — data-tour в MiniRepertory

**Файл:** `src/app/patients/[id]/consultations/[consultationId]/MiniRepertory.tsx`

Прочитать файл полностью, затем добавить:

| Атрибут | Где искать |
|---------|-----------|
| `data-tour="mini-search"` | `<input ... placeholder={t(lang).miniRepertory.search}` |
| `data-tour="mini-results"` | Контейнер с результатами поиска (список рубрик) |
| `data-tour="mini-analysis"` | `<div>` панели анализа (условный, рендерится при `showAnalysis && analysisEntries.length > 0`) |
| `data-tour="mini-assign"` | Кнопка «Rx» / назначить препарат (внутри топ-препаратов, guard: `onAssignRemedy &&`) |

**Важно:** `mini-analysis` рендерится только при наличии рубрик в анализе. Тур учтёт это — сначала добавим рубрику, потом покажем панель.

---

## Task 7 — data-tour в RepertoryClient

**Файл:** `src/app/repertory/RepertoryClient.tsx`

Прочитать файл, затем добавить:

| Атрибут | Где искать |
|---------|-----------|
| `data-tour="rep-search"` | `<input ... value={query}` (поле поиска в шапке) |
| `data-tour="rep-rubric-row"` | Корневой элемент компонента `RubricRow` (найти его определение в этом же файле) |
| `data-tour="rep-add-rubric"` | Кнопка добавления рубрики в анализ внутри `RubricRow` |
| `data-tour="rep-eliminate"` | Кнопка «E» (элиминация) в панели анализа |
| `data-tour="rep-analysis"` | Корневой `<div>` боковой панели анализа |
| `data-tour="rep-top-remedies"` | `<div>` секции топ препаратов |

**Важно:** `RubricRow` — это подкомпонент внутри файла. Прочитать файл до конца чтобы найти его определение.

---

## Task 8 — Build verification

```bash
cd c:/projects/casebook && npm run build
```

Ожидаемый результат: нет TypeScript ошибок. Если есть — исправить до завершения фазы.

---

## Completion Checklist

- [ ] Task 1: IntakeLinkButton добавлен на страницу пациента (import + JSX)
- [ ] Task 2a: `data-tour="intake-link"` на IntakeLinkButton
- [ ] Task 2b: `data-tour="schedule-btn"` на ScheduleButton (свёрнутое состояние)
- [ ] Task 3a: `data-tour="complaints"` на обоих textarea в ConsultationEditor
- [ ] Task 3b: `data-tour="finish-btn"` на кнопке завершения
- [ ] Task 4: `data-tour="open-repertory"` на кнопке тулбара
- [ ] Task 5: `data-tour="inline-rx"` на корневом div InlineRx
- [ ] Task 6: 4 атрибута в MiniRepertory (mini-search, mini-results, mini-analysis, mini-assign)
- [ ] Task 7: 6 атрибутов в RepertoryClient (rep-search, rep-rubric-row, rep-add-rubric, rep-eliminate, rep-analysis, rep-top-remedies)
- [ ] Task 8: `npm run build` проходит без ошибок
